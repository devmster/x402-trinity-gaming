node_modules/
dist/
*.tgz

# Test suites and verification harnesses stay local, as in x402 trinity.
test/
unity/Tests/
unity/verify/
unreal/verify/

# Dev harnesses that spend real money and read keys from ~/.x402-secrets.
# These must never reach a public repo.
tools/mainnet-*.mjs

# Runtime state written by the library and by test runs.
.x402-*
.fee-*
.tab-*

# Build output from the engine adapters.
unity/verify/bin/
unity/verify/obj/
unreal/verify/*.obj
unreal/verify/*.exe
unreal/verify/*.pdb
unreal/Binaries/
unreal/Intermediate/
